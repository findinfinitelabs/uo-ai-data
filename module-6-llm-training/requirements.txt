# Core ML — CPU-compatible, no CUDA required
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0

# Fine-tuning
peft>=0.7.0
trl>=0.7.0

# Data
datasets>=2.15.0
numpy>=1.24.0

# AWS / DynamoDB (for prepare_data.py --source dynamodb)
boto3>=1.26.0

# Hugging Face
huggingface-hub>=0.19.0

# Utilities
pyyaml>=6.0.0
tqdm>=4.66.0
